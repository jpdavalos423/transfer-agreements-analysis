# apps/web_react

React + Vite migration scaffold (Phase 8 baseline). This does not replace `apps/web` yet.

Install dependencies:

```bash
cd apps/web_react
npm install
```

Run dev server:

```bash
npm run dev
```
