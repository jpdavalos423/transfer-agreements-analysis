# apps/api_fastapi

FastAPI migration scaffold (Phase 8 baseline). This does not replace `apps/api` yet.

Run (after installing migration deps):

```bash
uvicorn apps.api_fastapi.main:app --host 127.0.0.1 --port 8100 --reload
```
